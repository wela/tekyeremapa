# Contact page image

Drop your photo here as:

- contact.jpg — the large image beside the contact details

Any common extension works (.jpg, .png, .webp). Until the file exists a
placeholder shows. Path is set in components/contact/ContactInfo.tsx.
