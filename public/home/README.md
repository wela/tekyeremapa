# Home hero image

Drop any image in this folder and the home hero uses it. Filename does not
matter, any common extension works (.jpg, .png, .webp).

If this folder is empty, the hero falls back to the first photo in
/public/gallery/2025/, so it is never blank.
