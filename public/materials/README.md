# Hackathon materials

Drop the two presentation files here using these exact filenames:

- teams-outreach-presentation.pptx — "Teams Outreach Presentation"
- teams-outreach-presentation-2.pptx — second outreach deck (rename once its
  real title is known)

Titles, descriptions, and filenames are set in `lib/edition-2025.ts`
(`materials`). The second deck's title is a placeholder — update `title` and
`file` together there when you have the real name.
